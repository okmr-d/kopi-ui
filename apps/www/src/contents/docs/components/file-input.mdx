---
title: File Input
description: Displays a file input field.
component: true
---

<ComponentPreview
  name="file-input-default"
  className="[&_input]:max-w-xs"
  description="A form input component."
/>

## Installation

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="file-input" title="components/ui/file-input.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

## Usage

```tsx
import { FileInput } from "@/components/ui/file-input"
```

```tsx
<FileInput />
```

## Examples

### Variants

Use the `variant` prop to change the visual style of the input.

<ComponentPreview name="file-input-variants" className="[&_input]:max-w-xs" />

### Sizes

Use the `size` prop to change the size of the input.

<ComponentPreview name="file-input-sizes" className="[&_input]:max-w-xs" />

### Disabled

Use the `disabled` prop to disable the input.

<ComponentPreview name="file-input-disabled" className="[&_input]:max-w-xs" />

### Error

Use the `invalid` prop to change the input to an error state.

<ComponentPreview name="file-input-error" className="[&_input]:max-w-xs" />
